script.
  var queryDict = {};
  location.search.substr(1).split("&").forEach(function(item) {
    queryDict[item.split("=")[0]] = item.split("=")[1]
  });
  if (!window.opener._popdown) window.opener._popdown = {};
  window.opener._popdown[queryDict.state] = queryDict;
  window.close();
